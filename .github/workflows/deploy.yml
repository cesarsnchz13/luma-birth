name: Deploy to GoDaddy

on:
  push:
    tags:
      - 'v*'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger cPanel Git Deploy
        run: curl -s -X GET "${{ secrets.CPANEL_WEBHOOK_URL }}"
